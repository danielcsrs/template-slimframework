{% extends '_app/layout.twig' %}

{% block title %} Login {% endblock %}

{% block pageClass %}home{% endblock %}

{% block content %}

	<section>
		<div class="container">

			<div class="row">
        <div class="col-md-7 col-xs-12">
          <div class="panel mb-20">
            <div class="panel-body">
              <h4 class="mb-20">Sistema de Cadastros FGTKD</h4>
              <p>Bem-vindo! Gerencie e acompanhe seu cadastro na Federação.</h4>
              <p>Se você ainda não é filiado, você pode iniciar seu cadastro criando um usuário de acesso, após concluir o cadastro poderá solicitar a sua inscrição como Atleta, Mestre, Academia ou Clube.</p>
              <p>Em caso de dúvidas, entre em contato conosco pelo site.</p>
            </div>
          </div>
        </div>
				<div class="col-md-5 col-xs-12">
          <div class="panel">
            <div class="panel-body">

              <div class="header-page">
                <h1 class="mb-20">Login</h1>
                <p>Informe seus dados para entrar no sistema.</p>
              </div>
              <form action="{{ path_for("auth.login") }}" method="post">
                <div class="form-group{{ errors.email ? ' has-error' : '' }}">
                  <label for="email">E-mail</label>
                  <input type="email" class="form-control" id="email" name="email" value="{{ old.email }}" placeholder="Email" required>
                  {% if errors.email %}
                  <span class="help-block">{{ errors.email | first }}</span>
                  {% endif %}
                </div>
                <div class="form-group{{ errors.password ? ' has-error' : '' }}">
                  <label for="senha">Senha</label>
                  <input type="password" class="form-control" id="senha" name="senha" placeholder="Password" required>
                  {% if errors.password %}
                  <span class="help-block">{{ errors.password | first }}</span>
                  {% endif %}
                </div>
                <button type="submit" class="btn btn-primary btn-block">Acessar o Sistema</button>
                {{ csrf.field | raw }}
              </form>
              <br/>
              <a href="{{ path_for("auth.forgot") }}">Esqueci minha senha</a>
              <hr>
              <h3 class="mb-20">Ainda não tem cadastro?</h3>
              {# <p class="text-center mb-20">Ainda não tem cadastro?</p> #}
              <a class="btn btn-default btn-block" href="{{ path_for("auth.signup") }}">Clique aqui para criar seu usuário!</a>
            </div>
          </div>
        </div>

			</div>
		</div>
	</section>

{% endblock %}

{% block modal %}{% endblock %}
{% block scripts %}
{% endblock %}